"Act as a world-class Machine Learning educator. Your task is to create a detailed, beginner-friendly study guide in Markdown (.md) format based on provided lecture notes on Ensemble Learning.

Target Audience: A 'noob' who understands basic Python but is new to ML theory.

Requirements for the Content:

Clarity over Complexity: Use analogies to explain abstract concepts (e.g., explain Bagging like a committee of experts).

Depth: Don't just list definitions. Explain the 'Why.' (e.g., Why do we sample with replacement? Why does reducing correlation between trees improve the model?)

Math Simplified: When explaining formulas like the Bias-Variance tradeoff or the AdaBoost loss function, break down what each variable represents in plain English. Use LaTeX for all formulas.

Structure: Use a clear hierarchy with H1, H2, and H3 tags. Use bolding for key terms and callouts (blockquotes) for 'Pro-Tips' or 'Warnings.'

Algorithm Breakdown: For the Bagging and Random Forest algorithms, provide a step-by-step 'English' version of the pseudocode.

Please cover the following topics in depth:

The core philosophy of Ensemble Learning (Many vs. One).

Parallel vs. Sequential methods.

The mechanics of Bagging (Bootstrapping, Aggregation, and the 63% rule).

The Variance reduction math (Bias-Variance tradeoff).

Random Forest and the importance of feature randomness.

AdaBoost, exponential loss, and the intuition behind weight updates.

Format Guidelines:

Start with a 'Table of Contents.'

End with a 'Summary Table' comparing Bagging and Boosting.

Use standard Markdown formatting (no code blocks for prose)."